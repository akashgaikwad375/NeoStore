<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.webwerks.neostore">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.login.activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.login.activity.RegisterActivity" />
        <activity android:name=".view.home.activity.HomeActivity" />
        <activity android:name=".view.product.activity.ProductListingActivity" />
        <activity android:name=".view.address.activity.AddAddressActivity" />
        <activity android:name=".view.product.activity.ProductDetailsActivity" />
        <activity android:name=".view.address.activity.AddressListActivity" />
        <activity android:name=".view.myorder.activity.MyOrdersActivity" />
        <activity android:name=".view.myorder.activity.OrderIdActivity" />
        <activity android:name=".view.login.activity.ForgotPasswordActivity" />
        <activity android:name=".view.mycart.activity.MyCartActivity" />
        <activity android:name=".view.login.activity.ResetPasswordActivity" />
        <activity android:name=".view.myprofile.MyAccountActivity" />
        <activity android:name=".view.myprofile.EditProfileActivity"/>
    </application>

</manifest>